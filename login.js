/*function onSubmit(event){
    if(form.email.value.length == 0 || form.psw.value.length == 0)
    {
        const err = document.querySelector('#rm-hd');
        err.classList.remove('hidden');
        event.preventDefault();
    }
}

const form = document.forms['credentials'];
form.addEventListener('submit', onSubmit);


function isBackHome(event){
    window.location.href = "HW1.php"
}

const backbtn = document.querySelector('#back-arrow-btn');
backbtn.addEventListener('click', isBackHome);*/